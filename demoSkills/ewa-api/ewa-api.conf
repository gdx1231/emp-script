# EWA API 配置文件
# 

# ==================== API 配置 ====================

# API 基础地址（必填）
export EWA_API_URL="https://gdx/pf2023/servletApi"

# 登录凭据（必填）
export EWA_API_LOGIN_ID="gl"
export EWA_API_PASSWORD="gdx1231gdx"

# Token 缓存文件（可选）
export EWA_TOKEN_FILE="/tmp/.ewa_api_token"

# 认证模式: token, hmac, simple（可选，默认 token）
export EWA_AUTH_MODE="token"

# ==================== 数据库配置 ====================

# 默认数据库连接名称
export EWA_DEFAULT_DB="work"

# ==================== 输出配置 ====================

# 默认输出格式: json, xml, csv
export EWA_DEFAULT_OUTPUT="json"
